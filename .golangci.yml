version: '2'
run:
  timeout: 10m
  tests: true
linters:
  enable:
  - errcheck
  - gosimple
  - govet
  - ineffassign
  - staticcheck
  - unused
  - gofmt
  - goimports
  settings:
    goimports:
      local-prefixes: github.com/duynhne/user-service
